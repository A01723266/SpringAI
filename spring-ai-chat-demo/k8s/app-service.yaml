apiVersion: v1
kind: Service
metadata:
  name: spring-ai-chat-demo-service
  labels:
    app.kubernetes.io/name: spring-ai-chat-demo
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spring-ai-chat-demo
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 8080
      targetPort: 8080
